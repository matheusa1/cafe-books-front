import { SelectFeedback } from './parts/feedback'
import SelectLabel from './parts/label'
import { SelectMain } from './parts/main'
import { SelectRoot } from './parts/root'

export const Select = {
  Root: SelectRoot,
  Label: SelectLabel,
  Select: SelectMain,
  Feedback: SelectFeedback,
}
